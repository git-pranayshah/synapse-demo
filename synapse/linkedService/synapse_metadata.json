{
	"name": "synapse_metadata",
	"properties": {
		"parameters": {
			"baseurl": {
				"type": "string",
				"defaultValue": "[concat('https://',parameters('vaults_synapse_metadata_name'), uniqueString(resourceGroup().id),'.vault.azure.net/')]"
				
			}
		},
		"annotations": [],
		"type": "AzureKeyVault",
		"typeProperties": {
			"baseUrl": "@{linkedService().baseurl}"
		}
	}
}